import React from 'react';
import PropTypes from 'prop-types';

import './NavItems.css';

const NavItems = props => {
    return (
        <div>
            NavItems component!
        </div>
    );
};

NavItems.propTypes = {

};

export default NavItems;