import React from 'react';
import PropTypes from 'prop-types';

import './CardClassmates.css';

const CardClassmates = props => {
    return (
        <div>
            
        </div>
    );
};

CardClassmates.propTypes = {
    
};

export default CardClassmates;